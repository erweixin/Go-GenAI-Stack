# ============================================
# Sentry 监控服务环境变量模板
# ============================================
# 使用方式：
#   1. 复制此文件: cp env.template .env
#   2. 生成密钥: openssl rand -base64 32
#   3. 填写下面的配置
#   4. docker compose up -d
# ============================================

# ============================================
# Sentry 核心配置
# ============================================

# Sentry 密钥（必需）- 用于加密会话和数据
# 生成方式: openssl rand -base64 32
SENTRY_SECRET_KEY=your-secret-key-here-please-change-me

# PostgreSQL 密码（必需）
# 生成方式: openssl rand -base64 16
SENTRY_POSTGRES_PASSWORD=your-postgres-password-here

# ============================================
# 服务端口配置
# ============================================

# Sentry Web 端口（默认 9000）
SENTRY_PORT=9000

# ============================================
# URL 配置
# ============================================

# Sentry 访问 URL（用于邮件链接、通知等）
# 开发环境: http://localhost:9000
# 生产环境: https://sentry.yourdomain.com
SENTRY_URL_PREFIX=http://localhost:9000

# ============================================
# 邮件配置（可选）
# ============================================

# 邮件后端
# - console: 输出到控制台（开发环境）
# - smtp: 使用 SMTP 发送（生产环境）
SENTRY_EMAIL_BACKEND=console

# SMTP 配置（当 BACKEND=smtp 时需要）
SENTRY_EMAIL_HOST=smtp.gmail.com
SENTRY_EMAIL_PORT=587
SENTRY_EMAIL_USER=your-email@gmail.com
SENTRY_EMAIL_PASSWORD=your-email-password
SENTRY_EMAIL_USE_TLS=true
SENTRY_SERVER_EMAIL=sentry@localhost

