-- ============================================
-- Go-GenAI-Stack 生产环境数据库初始化脚本
-- ============================================
-- 此脚本仅在首次创建数据库时执行
-- 后续的 Schema 迁移应使用 Atlas 或其他迁移工具
-- ============================================

-- 创建必要的扩展
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pg_trgm";  -- 用于文本搜索

-- 创建数据库（如果不存在）
-- 注意：通常由 POSTGRES_DB 环境变量自动创建

-- 设置时区
SET timezone = 'UTC';

-- 显示版本信息
SELECT version();

-- ============================================
-- 注意事项
-- ============================================
-- 1. Schema 迁移应使用 Atlas 或 golang-migrate
-- 2. 不要在此文件中创建表结构
-- 3. 此文件仅用于设置数据库级别的配置
-- ============================================

