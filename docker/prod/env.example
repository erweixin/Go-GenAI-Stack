# ============================================
# Go-GenAI-Stack 生产环境配置
# ============================================
# 
# 🎯 极简配置 - 只需修改密码即可启动
# 其他配置使用合理的默认值（可在此文件中覆盖）
# 
# ⚠️  必须修改的配置（3 项）：
#   - POSTGRES_PASSWORD
#   - REDIS_PASSWORD
#   - APP_JWT_SECRET
# 
# 💡 生成安全密钥：
#   openssl rand -base64 32  # 用于 APP_JWT_SECRET
#   openssl rand -base64 24  # 用于密码
#
# 📊 监控服务：
#   监控服务（Sentry/Jaeger/Prometheus/Grafana）已迁移到 docker/monitoring
#   请参考: docker/monitoring/env.template
# ============================================

# ============================================
# 🔐 必需配置（必须修改）
# ============================================
# 数据库密码（建议 24+ 字符）
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Redis 密码（建议 24+ 字符）
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD

# JWT 密钥（建议 32+ 字符）
APP_JWT_SECRET=CHANGE_ME_JWT_SECRET_KEY

# ============================================
# 🔧 环境配置
# ============================================
# 环境标识（production/staging/development）
APP_ENV=production

# 数据库 SSL 模式（生产环境必须启用）
# 可选值: disable, require, verify-ca, verify-full
APP_DATABASE_SSL_MODE=require

# ============================================
# 📊 核心开关
# ============================================
# 日志级别（debug/info/warn/error）
# 生产环境推荐: info 或 warn
APP_LOGGING_LEVEL=info

# 是否启用分布式追踪（true/false）
APP_MONITORING_TRACING_ENABLED=true

# 追踪采样率（0.0-1.0）
# 0.1 表示 10% 的请求会被追踪
APP_MONITORING_SAMPLE_RATE=0.1

# ============================================
# 🌐 服务端口（可选）
# ============================================
# 应用版本标识
VERSION=v1.0.0

# 后端 API 端口（映射到宿主机）
APP_PORT=8080

# ============================================
# 📚 高级配置说明
# ============================================
# 
# 以上配置已足够启动生产环境。如需自定义，可在下方添加任何配置项。
# 完整配置列表请参考: README.md 或 docker-compose.yml
# 
# 常用高级配置示例：
# 
# 数据库连接池:
#   APP_DATABASE_MAX_OPEN_CONNS=50
#   APP_DATABASE_MAX_IDLE_CONNS=10
#   APP_DATABASE_CONN_MAX_LIFETIME=10m
# 
# Redis 连接池:
#   APP_REDIS_POOL_SIZE=20
#   APP_REDIS_MIN_IDLE_CONNS=10
# 
# 服务器超时:
#   APP_SERVER_READ_TIMEOUT=60s
#   APP_SERVER_WRITE_TIMEOUT=60s
# 
# 日志配置:
#   APP_LOGGING_FORMAT=json
#   APP_LOGGING_OUTPUT=file
#   APP_LOGGING_MAX_SIZE=200
# 
# LLM 配置:
#   APP_LLM_PROVIDERS_OPENAI=sk-...
#   APP_LLM_DEFAULT_MODEL=gpt-4o
# 
# 更多配置请参考: docker-compose.yml 的 environment 部分
# ============================================
