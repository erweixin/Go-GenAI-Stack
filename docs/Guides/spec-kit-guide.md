# Spec-Kit 使用指南

> 📋 规范驱动开发（Spec-Driven Development）完整指南

**最后更新**: 2025-12-19

---

## 1. 什么是 Spec-Kit？

**Spec-Kit** 是 GitHub 官方开发的规范驱动开发工具包，通过"规范"来驱动需求、设计、开发和协作流程。

### 1.1 核心理念

- **规范即代码**：将业务需求转化为可执行的开发规范
- **四阶段流程**：Specify → Plan → Tasks → Implement
- **强制 TDD**：确保代码质量和需求一致性
- **AI 友好**：与 AI 编码工具无缝集成

### 1.2 工作流程

```
┌─────────────┐
│  Specify    │  编写业务规范
│  (规格化)   │  .spec-kit/specs/*.spec.md
└──────┬──────┘
       ↓
┌─────────────┐
│  Plan       │  生成实现计划
│  (规划)     │  .spec-kit/plans/*.plan.md
└──────┬──────┘
       ↓
┌─────────────┐
│  Tasks      │  分解任务
│  (任务)     │  .spec-kit/tasks/*.tasks.md
└──────┬──────┘
       ↓
┌─────────────┐
│  Implement  │  实现代码
│  (实现)     │  domains/*/usecases.yaml → 代码
└─────────────┘
```

---

## 2. 快速开始

### 2.1 安装 Spec-Kit

```bash
# 方式 1: 使用 npm (如果使用官方 CLI)
npm install -g @github/spec-kit

# 方式 2: 手动创建目录结构（推荐，更灵活）
mkdir -p .spec-kit/{specs,plans,tasks,templates}
```

### 2.2 创建第一个规范

```bash
# 1. 复制模板
cp .spec-kit/templates/spec.template.md .spec-kit/specs/my-feature.spec.md

# 2. 编辑规范
vim .spec-kit/specs/my-feature.spec.md

# 3. 创建实现计划
cp .spec-kit/templates/plan.template.md .spec-kit/plans/my-feature.plan.md

# 4. 分解任务
cp .spec-kit/templates/tasks.template.md .spec-kit/tasks/my-feature.tasks.md
```

---

## 3. 规范编写指南

### 3.1 Spec（业务规范）

**位置**: `.spec-kit/specs/*.spec.md`

**内容**:
- 业务目标
- 用户故事
- 功能需求
- 非功能需求
- 约束条件

**示例**:

```markdown
# Task Management Domain Specification

## 1. 业务目标
用户需要一个简单、高效的任务管理系统...

## 2. 用户故事
#### US-1: 创建任务
**作为** 用户  
**我希望** 能够创建新任务  
**以便** 记录我需要完成的工作

**验收标准**:
- ✅ 用户可以输入任务标题（必填，1-200 字符）
- ✅ 用户可以设置优先级（low/medium/high）

## 3. 功能需求
### 3.1 任务实体
- task_id: string (UUID)
- title: string (1-200 字符)
- ...
```

### 3.2 Plan（实现计划）

**位置**: `.spec-kit/plans/*.plan.md`

**内容**:
- 架构设计
- 数据模型设计
- API 设计
- 业务逻辑实现方案
- 测试策略

**示例**:

```markdown
# Task Management Domain Implementation Plan

## 1. 架构设计
采用 DDD 三层架构：
- Handler 层（HTTP 适配）
- Service 层（业务逻辑）
- Repository 层（数据访问）

## 2. 数据模型设计
```sql
CREATE TABLE tasks (
    id VARCHAR(36) PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    ...
);
```

## 3. API 设计
| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/tasks` | POST | 创建任务 |
```

### 3.3 Tasks（任务分解）

**位置**: `.spec-kit/tasks/*.tasks.md`

**内容**:
- 任务列表
- 任务详细描述
- 验收标准
- 依赖关系

**示例**:

```markdown
# Task Management Domain - Task Breakdown

## 任务概览
| 任务 ID | 任务名称 | 优先级 | 状态 | 预计工时 |
|---------|---------|--------|------|---------|
| TASK-001 | 数据库表结构设计 | High | ✅ Done | 2h |
| TASK-002 | 领域模型实现 | High | ✅ Done | 4h |

## 详细任务分解
### TASK-001: 数据库表结构设计
**描述**: 设计 tasks 表结构...

**验收标准**:
- [x] 表结构符合业务需求
- [x] 所有约束条件正确
```

---

## 4. 与 usecases.yaml 集成

### 4.1 从 Spec 到 usecases.yaml

**Spec-Kit Spec** (业务层):
```markdown
US-1: 创建任务
- 用户可以输入任务标题（必填，1-200 字符）
- 用户可以设置优先级（low/medium/high）
```

**usecases.yaml** (技术层):
```yaml
CreateTask:
  description: "创建一个新的任务"
  http:
    method: POST
    path: /api/tasks
  input:
    title:
      type: string
      required: true
      validation: "max=200,min=1"
    priority:
      type: string
      validation: "oneof=low medium high"
```

### 4.2 同步流程

```
1. 编写 Spec-Kit spec
   ↓
2. 生成 plan（技术方案）
   ↓
3. 更新 usecases.yaml（实现规范）
   ↓
4. AI 根据 usecases.yaml 生成代码
```

---

## 5. 最佳实践

### 5.1 Spec 编写

✅ **推荐做法**:
- 使用清晰的用户故事格式
- 明确定义验收标准
- 包含非功能需求
- 记录依赖关系

❌ **避免做法**:
- 过度技术细节（应放在 plan 中）
- 模糊的需求描述
- 缺少验收标准

### 5.2 Plan 编写

✅ **推荐做法**:
- 详细的架构设计
- 清晰的数据模型
- 完整的 API 设计
- 明确的实现步骤

❌ **避免做法**:
- 业务需求描述（应在 spec 中）
- 实现细节（应在代码中）

### 5.3 Tasks 编写

✅ **推荐做法**:
- 任务粒度适中（2-8 小时）
- 清晰的验收标准
- 明确的依赖关系
- 可追踪的状态

❌ **避免做法**:
- 任务过大（> 1 天）
- 缺少验收标准
- 依赖关系不明确

---

## 6. 工具和命令

### 6.1 常用命令

```bash
# 创建新规范
cp .spec-kit/templates/spec.template.md .spec-kit/specs/my-feature.spec.md

# 创建实现计划
cp .spec-kit/templates/plan.template.md .spec-kit/plans/my-feature.plan.md

# 创建任务分解
cp .spec-kit/templates/tasks.template.md .spec-kit/tasks/my-feature.tasks.md

# 查看所有规范
ls -la .spec-kit/specs/

# 查看所有计划
ls -la .spec-kit/plans/
```

### 6.2 规范状态管理

在规范文件中使用状态标记：

```markdown
**状态**: 🚧 进行中 / ✅ 已实现 / 📋 已归档
```

### 6.3 版本管理

在规范文件中记录版本：

```markdown
**版本**: 1.0
**创建日期**: 2025-12-19
**最后更新**: 2025-12-19
```

---

## 7. 示例：完整流程

### Step 1: 编写 Spec

创建 `.spec-kit/specs/task-export.spec.md`:

```markdown
# Task Export Feature Specification

## 用户故事
US-1: 导出任务
**作为** 用户  
**我希望** 能够导出任务列表为 CSV 格式  
**以便** 在 Excel 中分析和处理

## 功能需求
- 支持导出为 CSV 格式
- 包含所有任务字段
- 支持筛选条件
```

### Step 2: 生成 Plan

创建 `.spec-kit/plans/task-export.plan.md`:

```markdown
# Task Export Implementation Plan

## API 设计
GET /api/tasks/export?format=csv&status=pending

## 实现方案
1. 在 TaskService 中添加 ExportTasks 方法
2. 使用 csv.Writer 生成 CSV 文件
3. 返回文件流
```

### Step 3: 更新 usecases.yaml

更新 `backend/domains/task/usecases.yaml`:

```yaml
ExportTasks:
  description: "导出任务列表"
  http:
    method: GET
    path: /api/tasks/export
  input:
    format:
      type: string
      default: "csv"
      validation: "oneof=csv json"
  steps:
    - ValidateInput
    - QueryTasks
    - FormatExport
    - ReturnFile
```

### Step 4: 实现代码

AI 根据 `usecases.yaml` 自动生成代码。

---

## 8. 常见问题

### Q1: Spec-Kit 和 usecases.yaml 有什么区别？

**A**: 
- **Spec-Kit**: 业务层规范，面向产品和业务
- **usecases.yaml**: 技术层规范，面向 AI 和开发

### Q2: 必须使用 Spec-Kit 吗？

**A**: 不是必须的。Spec-Kit 提供更好的规范管理，但也可以直接使用 usecases.yaml。

### Q3: 如何与现有功能集成？

**A**: 
- 新功能：使用完整流程（Spec-Kit → usecases.yaml → 代码）
- 现有功能：可以逐步迁移，或保持现状

### Q4: Spec-Kit 规范需要版本控制吗？

**A**: 是的，建议使用 Git 管理所有规范文件。

---

## 9. 参考资料

- [Spec-Kit 官方文档](https://github.com/github/spec-kit)
- [与现有系统集成指南](./spec-kit-integration.md)
- [Task Domain 示例](../../.spec-kit/specs/task-management.spec.md)
- [usecases.yaml 格式说明](../../backend/domains/task/usecases.yaml)

---

## 10. 下一步

1. ✅ 阅读本指南
2. 📋 查看 [Task Domain 示例](../../.spec-kit/specs/task-management.spec.md)
3. 🚀 创建你的第一个规范
4. 📖 阅读 [集成指南](./spec-kit-integration.md)

---

**最后更新**: 2025-12-19  
**维护者**: Go-GenAI-Stack Team

